# セキュリティポリシー

## サポート対象

`md-peruse` は初回リリース前です。現時点では `main` ブランチのみをサポート対象とします。リリース後は最新の安定版のみをサポートします。

## 脆弱性の報告

**公開Issueへ脆弱性を投稿しないでください。**

報告は GitHub の非公開経路で受け付けます。

- [Security Advisories から報告する](https://github.com/scottlz0310/md-peruse/security/advisories/new)

報告に次の情報を含めてください。

- 影響を受けるバージョン、またはコミットハッシュ
- 再現手順（再現に必要なMarkdown、画像、フォルダー構成を含む）
- 想定される影響範囲
- 判明している場合は原因箇所

初回応答は7日以内を目安とします。個人開発のため、修正までの期間は内容によって変動します。

## 想定する脅威

`md-peruse` は閲覧専用のMarkdownビューワーであり、次を前提としています。

- 閲覧対象のMarkdownと画像は**信頼できない入力**として扱う
- 対象ファイルへ書き込まない
- ネットワークアクセスを行わない
- テレメトリを送信しない

したがって、次に該当する報告を脆弱性として扱います。

| 分類 | 例 |
| --- | --- |
| sanitizeの迂回 | Markdown経由でWebView上のスクリプトが実行される |
| ワークスペース境界の突破 | 選択したルートフォルダー外のファイルが読み取られる |
| パス検証の回避 | `..`、UNCパス、device path、reparse pointによる境界外アクセス |
| 危険なURL scheme | `javascript:`、`ms-` などのschemeが実行される |
| 意図しない書き込み | 閲覧対象またはその他のファイルが変更される |
| サービス拒否 | 特定の入力でアプリが応答不能になり、復帰できない |

設計上の安全方針は [docs/design-decisions.md](./docs/design-decisions.md) の第7章と第8章に記載しています。

## 脆弱性として扱わないもの

- 不正なMarkdownやMermaidの構文エラーが、該当箇所のみのエラー表示として現れること（部分的失敗の分離は仕様です）
- 上限値（Markdown 10 MiB、画像32 MiBなど）を超える入力が拒否されること
- WebView2 Runtime自体の脆弱性（Microsoftへ報告してください）
