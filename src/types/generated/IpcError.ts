// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { ErrorCode } from "./ErrorCode";

/**
 * IPCの失敗。
 *
 * 表示場所（ネイティブダイアログ、プレビュー領域、ツリー項目、文書内要素）は
 * Frontendが呼び出しの文脈から決める。同じcodeでも、タブを開く操作で起きたか
 * ツリーの走査で起きたかによって表示先が変わるためである（design-decisions.md 12）。
 */
export type IpcError = { code: ErrorCode, 
/**
 * 表示用のメッセージ。現在のUI言語（10.5）で組み立てる。
 *
 * Rust側が言語を持つのは、ネイティブメニューとネイティブダイアログの文言を
 * どのみちRust側が持つためである。IPCが成立しない場面（WebView2 Runtimeの欠落）の
 * 表示もRust側で行う以上、辞書を両側へ分けると同じ文言が二重になる。
 *
 * 言語を切り替える直前に発行した要求の応答は、旧言語のまま届きうる。これは
 * 受け入れる。エラー表示は一時的であり、同じ操作をやり直せば新しい言語になる。
 * 応答へ言語を添えてFrontendが捨てる仕組みは、失敗の表示のために往復を増やす
 * 割に得るものがない。Frontendは `message` の文字列比較で分岐しない。
 *
 * ネイティブ絶対パスを含めない。対象を示すときはワークスペースルートからの
 * 相対パスを使う（design-decisions.md 5.3、7.1）。
 */
message: string, 
/**
 * 補足情報。原因の切り分けに使う。`message` と同じくネイティブ絶対パスを含めない。
 */
detail: string | null, };
